#define VK_USE_PLATFORM_MACOS_MVK
#include "vulkan_platform.h"

namespace orion::vulkan
{
    VkSurfaceKHR create_platform_surface(VkInstance instance, const Window& window)
    {
        // TODO: Create VkSurfaceKHR from macos window
    }
} // namespace orion::vulkan
